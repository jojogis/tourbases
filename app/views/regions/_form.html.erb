<div class="row">
  <div class="col-md-5">
    <%= form_with model: @region do |form| %>
      <div>
        <%= form.label :name,"Имя",class:"form-label",required:true %>
        <%= form.text_field :name,class:"form-control" + " is-invalid"*@region.errors.full_messages_for(:name).length %>
        <% puts @region.errors.full_messages_for(:name).each do |message| %>
          <div class="invalid-feedback"><%= message %></div>
        <% end %>
      </div>
      <br/>
      <div>
        <label>Страна</label>
        <%= form.collection_select :country_id, Country.all, :id, :name,{},{class: "form-select"} %>
      </div>
      <br/>
      <div>
        <%= form.submit "Сохранить",class:"btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>