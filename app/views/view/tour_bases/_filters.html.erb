<h5>Фильтры</h5>
<br/>
<div class="accordion">
  <div class="accordion-item">
    <h2 class="accordion-header" id="headingOne">
      <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
        Страны
      </button>
    </h2>
    <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne">
      <div class="accordion-body">
        <% @countries.each do |country| %>
          <%= link_to country.name, view_country_tour_bases_path(country),class:country==@country ? "link-dark" : "" %><br/>
        <% end %>
      </div>
    </div>
  </div>
  <div class="accordion-item">
    <h2 class="accordion-header" id="headingTwo">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
        Регионы
      </button>
    </h2>
    <div id="collapseTwo" class="accordion-collapse collapse" aria-labelledby="headingTwo">
      <div class="accordion-body">
        <% @regions.each do |region| %>
          <%= link_to region.name, view_country_region_tour_bases_path(region.country,region),class:region==@region ? "link-dark" : "" %><br/>
        <% end %>
      </div>
    </div>
  </div>
  <div class="accordion-item">
    <h2 class="accordion-header" id="headingThree">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseThree" aria-expanded="false" aria-controls="collapseThree">
        Города
      </button>
    </h2>
    <div id="collapseThree" class="accordion-collapse collapse" aria-labelledby="headingThree">
      <div class="accordion-body">
        <% @cities.each do |city| %>
          <%= link_to city.name, view_country_region_city_tour_bases_path(city.region.country,city.region,city),class:city==@city ? "link-dark" : "" %><br/>
        <% end %>
      </div>
    </div>
  </div>
</div>